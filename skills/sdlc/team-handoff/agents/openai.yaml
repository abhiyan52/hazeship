interface:
  display_name: "Team Handoff"
  short_description: "Create or close a durable question handoff to the human team"
  default_prompt: "Use $team-handoff to raise a question for the team about this gap."
policy:
  allow_implicit_invocation: false
