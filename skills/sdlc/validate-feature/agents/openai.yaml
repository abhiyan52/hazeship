interface:
  display_name: "Validate Feature"
  short_description: "Verify every requirement was actually built before the final PR"
  default_prompt: "Use $validate-feature to run final validation on this feature."
policy:
  allow_implicit_invocation: false
