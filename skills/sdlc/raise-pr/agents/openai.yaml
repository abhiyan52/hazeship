interface:
  display_name: "Raise PR"
  short_description: "Open slice/final PRs via gh and record merges in the manifest"
  default_prompt: "Use $raise-pr to raise the PR for this slice."
policy:
  allow_implicit_invocation: false
