# Blueprint — `<use-case>`

**When to reuse this:** `<the concrete situation this applies to>`

**First established:** `<YYYY-MM-DD>` · **Last confirmed:** `<YYYY-MM-DD>`

## Steps

1. `<step, with the exact command where there is one>`
2. `<step>`

## Commands

```bash
<copy-pasteable commands, with placeholders for anything project-specific>
```

## Guardrails

- `<what went wrong before, or what must not be done here>`

## Known gaps

- `<what this blueprint does not cover yet>`
