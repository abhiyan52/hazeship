# Runbook — deploy to `<environment>`

**Target:** `<host label / service — connection facts live in repo-map, not here>`
**First established:** `<YYYY-MM-DD>` · **Last deployed with:** `<YYYY-MM-DD>`
**Approval required:** `<who must give the go>`

## Preconditions

- `<branch/ref that deploys to this environment>`
- `<test/build gate that must pass>`

## Steps

1. `<step, with the exact command>`   <!-- append UNVERIFIED if never executed -->
2. `<step>`

## Verification

- `<command or check, and what output means success>`

## Rollback

1. `<exact step to return to the previous known-good state>`

## Known gaps

- `<what this runbook does not cover yet>`
